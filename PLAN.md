# pi-statusline — Plan

The working plan lives at `.claude/20260921/PLAN-10-01-42.md` (gitignored, local to this
machine) in the `01-plan` template format that `02-implement` consumes. That file is the
single source of truth for scope, slices, and decisions.

This stub exists so the repo root points at it. Do not restate the plan here — two copies
drift.
